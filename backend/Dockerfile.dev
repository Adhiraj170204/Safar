FROM node:22-alpine

# Required for bcrypt native compilation on Alpine
RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source is mounted via volume at runtime — no COPY needed here
EXPOSE 3000

CMD ["node", "--watch", "src/server.js"]
